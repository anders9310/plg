package plg.test.unit.process;

import org.junit.Test;
import plg.generator.process.CurrentGenerationState;

public class CurrentGenerationStateTest {
    @Test
    public void testPotential(){
        /*CurrentGenerationState state = new CurrentGenerationState(0,true,true);
        assert state.potential == 1;

        CurrentGenerationState state2 = state.increasePotentialBy(1);

        assert state.potential == 2;
        assert state2.potential == 2;*/
    }
}
